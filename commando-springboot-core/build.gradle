dependencies {
    api project(':commando-spring-core')
    api 'org.springframework.boot:spring-boot:'+springBootVersion
    testImplementation  group: 'org.springframework', name: 'spring-test', version: springVersion

}
